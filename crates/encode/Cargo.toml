[package]
name = "lowlat-encode"
version = "0.0.0"
publish = false
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true

[dependencies]
lowlat-common.workspace = true
# The device interfaces, resolved at runtime and shared with the decoders.
lowlat-drivers.workspace = true
lowlat-capture.workspace = true

[target.'cfg(target_os = "linux")'.dependencies]
# The third encoder path, under probe. One interface for import, conversion and
# encode on one device, which is what the other two cannot be.
ash.workspace = true

[lints]
workspace = true
